# Strands Agent Template

## CDK Conventions

- Prefer explicit imports over wildcards (`import { Stack } from "aws-cdk-lib"` not `import * as cdk from "aws-cdk-lib"`). Namespace imports for Node built-ins (e.g. `import * as path`) are fine.
